# 地图服务配置文件 - 生产环境
Name: mapserver-prod
Host: 0.0.0.0
Port: 8080
SignatureSecret: "MapServerSecretKey2026"

# MySQL 数据库配置（生产环境业务数据存储）
MySQL:
  DataSource: "go_readonly:HAIwaitan13!@tcp(172.22.164.13:3306)/container_house_production?charset=utf8mb4&parseTime=true&loc=UTC&timeout=5s&readTimeout=10s&writeTimeout=10s"

# 日志配置（生产环境）
Log:
  Mode: file             # 输出模式：file=落地文件，console=仅控制台
  Path: logs/prod        # 日志文件存储路径
  Level: info            # 记录等级
  KeepDays: 30           # 历史日志保留天数
  Compress: true         # 历史日志是否压缩成 gzip 包
